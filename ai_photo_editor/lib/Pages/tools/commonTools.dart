import 'package:flutter/material.dart';

class CommonTools extends StatelessWidget {
  const CommonTools({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
